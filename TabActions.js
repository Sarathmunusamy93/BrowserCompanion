class TabDetails{

    constructor(  tabID, tabURL, tabStart, isActive, numberOfTimesActive ){
       this.tabID = tabID,
        this.tabURL = tabURL,
        this.tabStart = tabStart,
        this.isActive = isActive,
        this.numberOfTimesActive = numberOfTimesActive
    }
}